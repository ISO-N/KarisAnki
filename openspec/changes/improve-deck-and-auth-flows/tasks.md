## 1. 认证表单

- [ ] 1.1 在 `frontend/lib/i18n.tsx` 增加显示/隐藏密码、确认密码、密码不一致等中英文文案
- [ ] 1.2 为登录和注册密码输入框添加显示/隐藏按钮，默认保持隐藏状态
- [ ] 1.3 为注册表单增加确认密码字段，并支持与密码相同的可见性切换
- [ ] 1.4 提交注册前校验两次密码一致，不一致时阻止请求并显示字段错误
- [ ] 1.5 检查密码输入框的 `aria-label`、按钮 `aria-pressed` 或等价可访问状态

## 2. 卡组列表交互

- [ ] 2.1 让卡组卡片可点击进入 `/decks/{id}`，且不破坏学习、重命名、重置、删除按钮的独立操作
- [ ] 2.2 将页面顶部的新建卡组内嵌表单改为按钮触发的 `Dialog`
- [ ] 2.3 空状态的新建卡组动作打开同一个弹窗
- [ ] 2.4 弹窗提交成功或取消后清理输入状态并刷新卡组列表
- [ ] 2.5 补充新建卡组弹窗所需的 i18n 文案并保持无障碍标签

## 3. 卡组详情弹窗与导入

- [ ] 3.1 将 `CardEditor` 改造为受控 `Dialog`，支持 `open` / `onOpenChange`
- [ ] 3.2 更新卡组详情页的新建和编辑调用点，保存或取消后正确清理编辑目标
- [ ] 3.3 将 `ImportCards` 从右侧 `Sheet` 改为居中 `Dialog`
- [ ] 3.4 为导入弹窗设置合理宽度、最大高度和垂直滚动，保证大 JSON 时解析按钮可访问
- [ ] 3.5 限制 JSON 输入框最大高度并支持手动调整，避免长文本把按钮推出可视区
- [ ] 3.6 保留导入预览的独立滚动和新增、删除行功能

## 4. 复习/学习会话

- [ ] 4.1 将评分按钮顺序调整为“忘记、模糊、熟悉”
- [ ] 4.2 同步调整键盘快捷键：`1=忘记`、`2=模糊`、`3=熟悉`
- [ ] 4.3 在 `StudySession` 增加 `completedCount`，加载队列时重置为 0
- [ ] 4.4 作答成功且 `response.completed` 为真时递增 `completedCount`
- [ ] 4.5 更新 `SessionHeader`，进度条使用完成数，右上角显示从 `1 / total` 开始递增的当前位置
- [ ] 4.6 重新开始或重新加载队列时重置进度状态

## 5. 验证

- [ ] 5.1 运行 `cd frontend && npm run lint`
- [ ] 5.2 运行 `cd frontend && npm run build`
- [ ] 5.3 手工验证登录/注册表单的密码切换与二次确认
- [ ] 5.4 手工验证卡组卡片进入详情、新建卡组弹窗、新建/编辑卡片弹窗
- [ ] 5.5 手工验证长 JSON 导入弹窗可滚动且解析、导入按钮可操作
- [ ] 5.6 手工验证评分按钮顺序、键盘映射和进度数字递增
