## 1. 基础环境

- [x] 1.1 在后端添加 `JPA`、`PostgreSQL`、`Flyway`、`Spring Security`、`Spring Session JDBC` 和参数校验依赖
- [x] 1.2 在前端添加 `Markdown`、消毒和 `KaTeX` 渲染依赖
- [x] 1.3 添加数据库连接、`Session Cookie`、邀请码和注册开关配置
- [x] 1.4 添加 `Next.js rewrite`，让开发和生产环境中的 `/api/*` 代理到后端
- [x] 1.5 添加后端和前端 `Dockerfile`，以及包含 `PostgreSQL`、后端、前端的 `Docker Compose`

## 2. 数据库 schema

- [x] 2.1 创建 `Flyway V1` 迁移，包含 `users`、`user_settings`、`sessions`、`decks`、`cards`、`card_states`、`answer_events`
- [x] 2.2 为 `decks` 和 `cards` 添加软删除字段
- [x] 2.3 为 `answer_events` 添加 `stage_before` 和 `stage_after`
- [x] 2.4 为 `card_states` 添加 `due_date`、`due_since`、`relearn_mode`、`relearn_correct_count`、`queue_type`
- [x] 2.5 添加用户归属、卡片查询、队列查询和统计查询所需的数据库索引
- [x] 2.6 为所有 v1 表添加 `JPA` 实体和 `Repository`

## 3. 账号认证与设置

- [x] 3.1 实现邀请码注册，包含邮箱归一化、密码校验和注册后自动登录
- [x] 3.2 实现邮箱/密码登录，使用 `Spring Session` 和 `HttpOnly Cookie`
- [x] 3.3 实现退出当前设备和退出所有设备
- [x] 3.4 实现修改密码，需要验证当前密码
- [x] 3.5 实现刷新时间、语言和主题的用户设置 API
- [x] 3.6 应用默认 04:00 刷新时间、15 分钟粒度和账号设置持久化
- [x] 3.7 添加注册/登录限流，不做永久锁定
- [x] 3.8 添加注册、重复邮箱、无效邀请码、登录、退出、修改密码和 `Session` 过期集成测试

## 4. 卡组与卡片管理

- [x] 4.1 实现卡组列表、创建、重命名、删除和重置 API
- [x] 4.2 实现卡片创建、编辑、删除和重置 API
- [x] 4.3 实现卡片列表按正反面进行不区分大小写的包含匹配搜索
- [x] 4.4 实现卡片状态筛选和每页 50 条分页
- [x] 4.5 实现单卡重置，使其进入新卡学习顺序最前面
- [x] 4.6 实现整组重置，并保留原始卡片加入顺序
- [x] 4.7 确保编辑卡片不改变调度和重学进度
- [x] 4.8 确保软删除卡片和卡组不会出现在列表、搜索、队列和预测中
- [x] 4.9 添加卡组/卡片归属、软删除、搜索、分页、重置和进度保留测试

## 5. 调度核心

- [x] 5.1 实现包含 `stage-1` 到 `stage9` 的 `stage` 状态机
- [x] 5.2 实现实际复习间隔映射 `1/2/4/7/15/30/90/180`
- [x] 5.3 实现新卡学习对熟悉、模糊、忘记三种结果的处理
- [x] 5.4 实现正常复习熟悉推进
- [x] 5.5 实现 `stage0`/`stage1` 和 `stage2` 到 `stage8` 的正常复习模糊行为
- [x] 5.6 实现正常复习忘记并重置到 `stage0`
- [x] 5.7 实现需要连续 5 次熟悉的忘记重学
- [x] 5.8 实现需要连续 3 次熟悉的模糊重学
- [x] 5.9 实现重学模式切换和计数清零规则
- [x] 5.10 实现 `stage8` 毕业和继续复习
- [x] 5.11 实现逾期卡复习不调整 `stage`
- [x] 5.12 添加覆盖所有 `stage`、结果、重学、毕业转换的表格化单元测试

## 6. 队列生成与答题 API

- [x] 6.1 实现学习队列生成，包含新卡和学习中触发的重学卡
- [x] 6.2 实现复习队列生成，包含到期的 `stage0` 到 `stage8` 卡片和复习中触发的重学卡
- [x] 6.3 实现重学卡按 `2^n` 张其他剩余卡片插入，并支持队尾兜底
- [x] 6.4 实现带 `queueType=LEARN/REVIEW` 的统一答题接口
- [x] 6.5 持久化重学模式和连续熟悉次数，刷新或重新进入后不丢失
- [x] 6.6 刷新时重新生成队列，不持久化队列快照
- [x] 6.7 处理活动队列中卡片被删除、重置或卡组被删除的情况
- [x] 6.8 按卡片 ID 返回最新内容，不把内容存入队列快照
- [x] 6.9 添加队列顺序、重学插入、刷新、删除、重置和过期队列冲突测试

## 7. 统计

- [x] 7.1 写入完整答题事件，包含用户、卡组快照、卡片、时间、时区、学习日、场景、作答前后 `stage` 和结果
- [x] 7.2 实现从 `stage-1` 到 `stage0` 转换的每日学习数量
- [x] 7.3 实现正常复习和重学作答的每日复习数量
- [x] 7.4 实现明天到期数量，重学卡每张只计一次
- [x] 7.5 实现阶段分布和独立的“重学中”数量
- [x] 7.6 实现忘记、模糊、熟悉总次数
- [x] 7.7 实现只使用正常复习数据、包含 `stage0` 的记忆保持率
- [x] 7.8 实现按当前时区划分的 0 到 23 小时时间段分布
- [x] 7.9 实现 7/30/90/180 天预测，包含逾期卡和重复未来复习
- [x] 7.10 实现全部卡组和单卡组筛选，包含已删除卡组历史
- [x] 7.11 添加覆盖重置、删除、重命名卡组和历史不可变的统计 API 测试

## 8. 前端应用

- [x] 8.1 实现登录和注册页面
- [x] 8.2 实现登录后的布局和卡组、统计、设置导航
- [x] 8.3 实现带新卡/重学/到期复习数量的卡组列表页
- [x] 8.4 实现卡组详情页，包含卡片列表、搜索、筛选、分页和开始按钮
- [x] 8.5 实现卡片创建/编辑界面，包含正反面 `Markdown` 编辑器和预览
- [x] 8.6 实现学习页，严格遵循“正面 → 翻面 → 选择结果”状态机
- [x] 8.7 实现复习页，使用相同答题状态机
- [x] 8.8 实现 `Space`、`1`、`2`、`3` 键盘控制
- [x] 8.9 实现带图表和卡组筛选的统计页
- [x] 8.10 实现密码、刷新时间、语言、主题和退出操作的设置页
- [x] 8.11 实现中文/英文国际化
- [x] 8.12 实现浅色/深色/跟随系统主题
- [x] 8.13 实现空状态、错误状态和队列失效处理
- [x] 8.14 实现桌面和移动端学习/复习流程的响应式布局

## 9. 部署与文档

- [x] 9.1 配置 `PostgreSQL`、后端、前端的 `Docker Compose` 服务
- [x] 9.2 编写数据库、邀请码和注册配置的环境变量文档
- [x] 9.3 编写单后端实例约束说明，避免并发执行 `Flyway` 迁移
- [x] 9.4 编写使用 `pg_dump` 备份和恢复 `PostgreSQL` 的文档
- [x] 9.5 编写生产环境同域代理配置文档

## 10. 验收与验证

- [x] 10.1 验证邀请码注册和登录
- [x] 10.2 验证卡组和卡片创建以及 `Markdown`/`LaTeX` 渲染
- [x] 10.3 验证新卡学习和 `stage0` 复习排期
- [x] 10.4 验证模糊和忘记重学流程
- [x] 10.5 验证正常复习按全部间隔推进
- [x] 10.6 验证 `stage8` 毕业和继续复习
- [x] 10.7 验证删除后历史统计保留
- [x] 10.8 验证时区和刷新时间变化只影响未来排期
- [x] 10.9 验证统计页数量、阶段分布、记忆保持率和预测
- [x] 10.10 运行后端测试、前端 lint/build 和完整验收场景
